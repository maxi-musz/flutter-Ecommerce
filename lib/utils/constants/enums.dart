/// List of Enums
/// They can't be created created inside a class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlePay,
  aoole,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}
